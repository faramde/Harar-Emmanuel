Harar Emmanuel
This aplication is build using Play Framwork.
Play is a high-productivity Java and Scala web application framework that integrates the components and APIs.
Play is based on a lightweight, stateless, web-friendly architecture and features. 
More control over the HTML output can be achieved when you are writing the markup as HTML to begin with, versus the JSF tags.
